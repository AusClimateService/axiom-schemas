from setuptools import setup, find_packages

setup(
    name='axiom_schemas',
    description='A package to manage schema versions for Axiom.',
    version='0.1.0',
    author='Ben Schroeter',
    author_email='ben.schroeter@csiro.au',
    packages=find_packages()
)